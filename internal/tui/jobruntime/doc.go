// Package jobruntime adapts the durable-jobs gateway client to Bubble Tea
// commands and typed result messages. It deliberately contains no view or
// model state so terminal frontends can choose their own interaction flow.
package jobruntime
